# Claude Code instructions

実装・レビューを始める前に、ルートの [`AGENTS.md`](./AGENTS.md) を読み、その制約を最優先してください。

本番成果物は `artifact.html` 1ファイルです。設計の正本は `docs/DESIGN.md` です。変更後は必ず次を実行します。

```bash
node scripts/validate.mjs
```
